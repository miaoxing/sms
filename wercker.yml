box: php
build:
  steps:
    - install-packages:
        packages: git
    - mbrevda/composer-install@1.0.1
    - script:
        name: PHPUnit integration tests
        code: phpunit --verbose --stderr